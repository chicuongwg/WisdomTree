// Placeholder Home (`/` — screen-inventory.md). The real screens (Home,
// Library, Stored Item Detail, Source Intake, My Submissions, Catalog,
// Catalog Item Detail, Librarian Desk) are step 2 of the demo brief.
export default function Home() {
  return (
    <main>
      <h1>WisdomTree</h1>
      <p>Scaffold (bước 1) — chờ review trước khi build các luồng demo.</p>
    </main>
  );
}
