# Module Boundaries

## Purpose
- Define the responsibility and ownership boundary of each major V1 subsystem.
- Prevent logic from leaking across app, worker, storage, and export layers.

## In Scope
- Web app, Source Repo, OCR/Parser worker, Tree service, Search/Graph, Export, Auth, Notifications, and operational modules.
- Data ownership, read/write responsibility, and integration boundary summary.

## Out of Scope
- Low-level class design.
- UI layout specifics.
- Infrastructure provisioning code.

## Decisions
- Business workflow orchestration belongs to the app, not external automation tools.
- OCR/parsing/AI-heavy work belongs to the worker runtime.
- Tree canonical Markdown belongs to the application database.
- Source binaries and evidence artifacts belong to object storage-backed Source Repo.

## Dependencies
- Context in [`system-context.md`](./system-context.md).
- Contracts in [`integration-contracts.md`](./integration-contracts.md).
- Lifecycle rules in [`data-model-lifecycle.md`](./data-model-lifecycle.md).

## Acceptance Criteria
- Every major responsibility is owned by one module.
- Engineers can tell which subsystem is allowed to mutate which kind of state.
- There is no ambiguity about where canonical data lives.

## Module Responsibility Map

| Module | Primary responsibility | Canonical data owned |
| --- | --- | --- |
| Web App + API | User interaction, workflow orchestration, authorization, tree editing, review actions | Tree nodes, branches, roles, tasks, audit, workflow state |
| Source Repo | Evidence storage surface for originals and extracted artifacts | Original files, raw text blobs, corrected text blobs, previews |
| OCR/Parser Worker | Parsing, OCR, AI draft generation, extraction jobs | Generated artifacts before app acceptance |
| Search/Graph | Query surface for tree and operational source discovery | Search index projections and graph projections |
| Export Service | Publish tree snapshots into the content repo | Export jobs and export manifests |
| Auth | Identity and session claims | Role assignment and session state |
| Notifications | In-app and email alerts | Notification records and delivery state |
| Board / Ops | Knowledge workflow tracking | Tasks, milestones, achievements |

## Ownership Rules
- Only the Web App + API may finalize:
  - source trust state
  - node verification state
  - branch completion
  - publish decisions
  - merge and archive actions
- Only the Worker may:
  - run OCR
  - run parser extraction
  - create AI suggestions and Markdown draft candidates
- Only the Export Service may write to the content repo.
- Object storage never acts as the business-rule engine; it stores artifacts addressed by the app.

## Data Mutation Boundaries
- Tree Markdown:
  - written by app
  - read by app, search, export
- Source binaries:
  - written on upload and re-upload
  - read by worker and Admin/Op download flow
- Raw extracted text:
  - written by worker
  - immutable after creation
- Corrected text:
  - written by assigned Editor or Admin/Op
  - finalized by Admin/Op review
- Markdown draft:
  - generated by worker
  - edited by assigned Editor or Admin/Op
  - published by Admin/Op

## Boundary Notes
- Search and graph are derived views; they do not own business truth.
- The content repo is a downstream artifact, not a collaborative authoring surface in V1.
- External automation such as n8n may trigger intake events later, but core lifecycle logic stays in the app and worker boundary.

